# Goal: libsvm-rs-4pz — API stability check vs published 0.8.1 (supervisor-driven)
Run cargo-semver-checks against the crates.io 0.8.1 baseline (default features
and --all-features); triage every finding; record outcome as a br comment.
Read-only on the crate. Details: br show libsvm-rs-4pz.
