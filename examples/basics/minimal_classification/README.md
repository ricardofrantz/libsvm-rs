# minimal_classification

Self-contained 4-point binary classification "hello world" using `libsvm-rs`.

## Run

```bash
bash examples/basics/minimal_classification/run.sh
```

This runs:

```bash
cargo run -p libsvm-rs --example minimal_classification
```
