# model_persistence

Train a model, save it to disk, load it back, and run prediction.

## Run

```bash
bash examples/api/model_persistence/run.sh
```

This runs:

```bash
cargo run -p libsvm-rs --example model_persistence
```
