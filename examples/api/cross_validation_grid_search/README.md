# cross_validation_grid_search

Simple `C`/`gamma` grid search with 5-fold cross-validation.

## Run

```bash
bash examples/api/cross_validation_grid_search/run.sh
```

This runs:

```bash
cargo run -p libsvm-rs --example cross_validation_grid_search
```
