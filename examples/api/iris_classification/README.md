# iris_classification

Run a standard multiclass Iris classification workflow using `data/iris.scale`.

## Run

```bash
bash examples/api/iris_classification/run.sh
```

This runs:

```bash
cargo run -p libsvm-rs --example iris_classification
```
